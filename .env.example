# Required for upstream infocepo services.
INFOCEPO_API_KEY=replace-me

# Strong token required from clients as Authorization: Bearer <token>.
API_TOKEN=replace-with-a-long-random-token

# MCP_HOST=0.0.0.0
# MCP_PORT=8085
# INFOCEPO_CREDENTIALS_FILE=~/.infocepo-credentials
